<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} [%thread] 
                %highlight(%-5level) %cyan(%logger{36}) - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Log everything at console level -->
    <root level="info">
        <appender-ref ref="Console" />
    </root>

    <!-- Change LoginController logs appear in color so we can distinct them -->
    <logger name="com.gcu.controller.LoginController" level="info" additivity="true">
        <appender-ref ref="Console" />
    </logger>

</configuration>



